# FolderBoy -- .gitignore

# User config file contains API keys and private paths.
# Never commit this file. Use FolderBoy.config.example.ps1 as a template.
FolderBoy.config.ps1

# Log files are written to the Logs\ subdirectory at runtime.
Logs/